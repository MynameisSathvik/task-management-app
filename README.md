E-Commerce Web App

Backend (Express + MongoDB) and Frontend (React + Vite)

Commands (from project root):

# Backend
cd backend
npm install
# create .env from .env.example
npm run seed
npm run dev

# Frontend
cd frontend
npm install
npm run dev

Environment variables are documented in .env.example files in root and frontend/.env.example
